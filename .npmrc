# EcoTrace usa pnpm como gestor de paquetes y Node 24 (ver .nvmrc)
auto-install-peers=true
strict-peer-dependencies=false
# No bloquear la instalación si la versión de Node local difiere; en CI/prod usar Node 24.
engine-strict=false
